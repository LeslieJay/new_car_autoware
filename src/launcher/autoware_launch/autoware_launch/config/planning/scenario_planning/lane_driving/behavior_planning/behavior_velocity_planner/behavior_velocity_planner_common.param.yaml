/**:
  ros__parameters:
    # Do not request more deceleration than common.param.yaml limit.min_acc.
    max_accel: -2.5
    max_jerk: -5.0
    system_delay: 0.5
    delay_response_time: 0.5
    is_publish_debug_path: false # publish all debug path with lane id in each module
